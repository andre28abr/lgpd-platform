[pytest]
testpaths = tests
python_files = test_*.py
# A raiz do projeto entra no sys.path: `pytest` (console script) e `python -m pytest`
# passam a importar app/seed/conteudo da mesma forma — no CI e em qualquer máquina.
pythonpath = .
