flask==3.1.1
flask-sqlalchemy==3.1.1
flask-migrate>=4.0.0
flask-login==0.6.3
flask-limiter>=3.0.0
werkzeug==3.1.3
python-dotenv==1.1.0
markdown>=3.6
bleach>=6.1
pymdown-extensions>=10.7
reportlab==4.4.0
gunicorn>=23.0.0

# Driver PostgreSQL — usado apenas quando DATABASE_URL aponta para Postgres.
# Em SQLite (padrão local) ele fica ocioso, mas instalar não atrapalha.
psycopg2-binary>=2.9.0

# Relatórios e segurança
openpyxl>=3.1.5          # exportação de relatórios em Excel
pyotp>=2.9.0             # 2FA (TOTP)
qrcode[pil]>=8.0         # QR Code para o enrolamento do 2FA
redis>=5.0.0             # opcional: storage do rate limit em produção (multi-worker)
feedparser>=6.0.0        # feed de legislação/notícias (ANPD)
