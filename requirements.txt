# Dependências de runtime — todas pinadas para builds reprodutíveis.
# Atualize de forma consciente (pip-audit no CI acusa CVEs conhecidos).
flask==3.1.3
flask-sqlalchemy==3.1.1
flask-migrate==4.1.0
flask-login==0.6.3
flask-limiter==4.1.1
werkzeug==3.1.6
python-dotenv==1.2.2
markdown==3.10.2
bleach==6.4.0
pymdown-extensions==11.0.1
reportlab==4.4.0
gunicorn==26.0.0

# Driver PostgreSQL — usado apenas quando DATABASE_URL aponta para Postgres.
# Em SQLite (padrão local) ele fica ocioso, mas instalar não atrapalha.
psycopg2-binary==2.9.12

# Relatórios e segurança
openpyxl==3.1.5          # exportação de relatórios em Excel
pyotp==2.10.0            # 2FA (TOTP)
qrcode[pil]==8.2         # QR Code para o enrolamento do 2FA
redis==8.0.0             # opcional: storage do rate limit em produção (multi-worker)
feedparser==6.0.12       # feed de legislação/notícias (ANPD)
